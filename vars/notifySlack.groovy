def call(String message, String channel = '#general') {
    echo "Sending notification to Slack channel: ${channel}"
    echo "Message: ${message}"
    // Add Slack integration logic here if needed, e.g., using Slack plugin
}
